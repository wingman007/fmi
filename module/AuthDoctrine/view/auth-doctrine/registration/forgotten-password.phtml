<h1>Please fill the form and hit the button.</h1>
<?php
$form = $this->form;
$form->setAttribute('action', $this->url('auth-doctrine/default', array('controller' => 'registration', 'action' => 'forgotten-password')));
$form->prepare();

echo $this->form()->openTag($form);
echo $this->formRow($form->get('usrEmail'));
echo $this->formSubmit($form->get('submit'));
echo $this->form()->closeTag();
?>