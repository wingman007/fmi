<h1>Please fill the form and hit the button.</h1>
<?php
$form = $this->form;
$form->setAttribute('action', $this->url('auth/default', array('controller' => 'registration', 'action' => 'forgotten-password')));
$form->prepare();

echo $this->form()->openTag($form);
echo $this->formRow($form->get('usr_email'));
echo $this->formSubmit($form->get('submit'));
echo $this->form()->closeTag();
?>